# Changelog

All notable changes to this project will be documented in this file.

## Unreleased

- Add deterministic closure/lifecycle control and immutable exact-context helpers without changing workflow-state ownership.
- Resolve verbatim hash-addressed Markdown sections with explicit legacy full-file fallback.
- Require precise planning references and continue safe Orchestrator operations in the same active turn.
- Add a six-call F002-shaped context benchmark with a 30% minimum byte-reduction gate while retaining independent review and retry semantics.
- Add the Owner-gated `to-spec` phase for synthesizing resolved discussion into the canonical Harness PRD.
- Replace stale `/run` references with the explicit `/harness run` command.
- Run Codex roles as App-native child tasks without an internal Codex CLI Runner.
- Start formal Builder retries in new sessions while keeping context repair in the current session.

## 0.1.0 - 2026-07-13

- Add the initial Codex plugin package metadata.
- Add deterministic, dependency-free package validation.
