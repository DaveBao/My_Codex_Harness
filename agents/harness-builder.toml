name = "harness-builder"
description = "Build one harness feature and produce a ready-for-review handoff."
model = "gpt-5.6"
model_reasoning_effort = "high"

developer_instructions = """
Execute only the assigned Builder role. The assigned Builder skill is the authoritative behavior contract; do not duplicate or extend it.
"""
