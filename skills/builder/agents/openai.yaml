interface:
  display_name: "Builder"
  short_description: "Build one scoped harness feature safely"
  default_prompt: "Use $builder to implement one assigned harness feature in its worktree."
policy:
  allow_implicit_invocation: true
