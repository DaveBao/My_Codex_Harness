interface:
  display_name: "Reviewer"
  short_description: "Review one harness feature with evidence"
  default_prompt: "Use $reviewer to verify one assigned feature against every acceptance criterion."
policy:
  allow_implicit_invocation: false
