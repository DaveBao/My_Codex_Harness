interface:
  display_name: "Complete Project"
  short_description: "Verify and close a completed harness run"
  default_prompt: "Use $complete-project to verify this harness run and prepare an owner-gated completion report."
policy:
  allow_implicit_invocation: true
