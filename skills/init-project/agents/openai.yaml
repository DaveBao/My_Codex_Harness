interface:
  display_name: "Init Project"
  short_description: "Initialize progressive-disclosure harness state"
  default_prompt: "Use $init-project to initialize this repository for progressive-disclosure agent workflows."
policy:
  allow_implicit_invocation: true
